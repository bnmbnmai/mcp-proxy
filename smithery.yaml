# Smithery.ai configuration https://smithery.ai/docs/config#smitheryyaml
startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction:
    |-
    (config) => ({
      "command": "npx",
      "args": [
        "-y", "@apollo_ai/mcp-proxy@latest"
      ]
    })
